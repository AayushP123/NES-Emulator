use nes_emulator::bus::Bus;
use nes_emulator::cart::Cart;
use nes_emulator::cpu::Cpu;

fn main() {
    println!("nestest runner placeholder");
    // TODO: load ROM, run CPU, compare against nestest.log
}